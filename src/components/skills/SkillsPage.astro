---
import { getEntry } from 'astro:content';
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import Icon from '../Icon.astro';
import FaqToggleIcon from '../FaqToggleIcon.astro';
import LocalPicture from '../LocalPicture.astro';
import SectionHeading from '../systems/SectionHeading.astro';
import Contact from './Contact.astro';
import LearningNeedRow from './LearningNeedRow.astro';
import marcel from '../../assets/team/marcel-original.png';
import routes from '../../content/routes.json';
const entry = await getEntry('skills', 'en');
if (!entry) throw new Error('Missing Skills content.');
const c = entry.data;
const servicesHeadingWord = c.services.heading.split(' ')[0];
const enquiry = (service: string) => `${routes.skills}?service=${service}#contact`;
---
<Layout title={c.title} description={c.description}>
  <section class="skills-hero shell" aria-labelledby="skills-heading">
    <div class="topline"><p class="draft-note">{c.draftLabel}</p></div>
    <div class="hero-grid">
      <div class="hero-copy">
        <h1 id="skills-heading">{c.hero.heading}<br /><span class="editorial">{c.hero.emphasis}</span></h1>
        <p class="hero-description">{c.hero.subline}</p>
        <div class="hero-actions"><a class="button button-dark button-metallic" href={`${routes.booking}?area=skills`}>{c.hero.primary}<Icon /></a><a class="text-link" href="#sample">{c.hero.secondary}</a></div>
        <p class="hero-note">{c.hero.note}</p>
      </div>
      <div class="hero-visual">
        <figure class="hero-photo"><LocalPicture src="/images/team/consultation-1280.webp" alt={c.hero.imageAlt} width={1280} height={724} eager responsive sizes="(min-width: 1024px) 42vw, (min-width: 768px) 44vw, calc(100vw - 40px)" /></figure>
      </div>
    </div>
    <nav class="page-nav" aria-label="Skills sections">{c.navigation.map(item => <a href={item.href}><span class="page-nav-label">{item.label}</span><span aria-hidden="true">↘</span></a>)}</nav>
  </section>

  <section class="trust shell" aria-labelledby="trust-heading" data-content-slot="trust">
    <div><h2 id="trust-heading">{c.trust.heading}</h2></div>
    <div class="trust-space"><span class="quote-mark editorial" aria-hidden="true">“</span><div><p>{c.trust.placeholder}</p><p class="small-note">{c.trust.note}</p></div></div>
  </section>

  <section class="section needs-section" id="needs" aria-labelledby="needs-heading"><div class="shell">
    <SectionHeading id="needs-heading" content={c.needs}>
      <span slot="emphasis" class="needs-heading-gradient">{c.needs.emphasis}</span>
    </SectionHeading>
    <div class="needs-list">{c.needs.items.map(item => <LearningNeedRow content={item} />)}</div>
  </div></section>

  <section class="section sample-section" id="sample" aria-labelledby="sample-heading" data-content-slot="course-sample"><div class="shell">
    <SectionHeading id="sample-heading" content={c.sample} />
    <div class="sample-grid"><div class="sample-space"><span class="tag">{c.sample.status}</span><div class="sample-art" aria-hidden="true"><span></span><span></span><span></span></div><div><h3>{c.sample.placeholder}</h3><p>{c.sample.note}</p></div></div><ol class="sample-steps">{c.sample.steps.map((step) => <li><div><h3>{step.name}</h3><p>{step.description}</p></div></li>)}</ol></div>
  </div></section>

  <section class="section shell" id="services" aria-labelledby="services-heading">
    <SectionHeading id="services-heading" content={{ heading: c.services.heading, emphasis: c.services.emphasis }}>
      <span slot="heading" class="services-heading-main"><span class="services-heading-metallic">{servicesHeadingWord}</span>{c.services.heading.slice(servicesHeadingWord.length)}</span>
    </SectionHeading>
    <div class="services-grid">
      {c.services.items.map(item => <article class:list={['service-card', item.accent, 'reveal-on-scroll']} id={`service-${item.id}`}>
        <span class="service-gradient" aria-hidden="true"></span>
        <h3>{item.name}</h3>
        <p class="service-description">{item.useCase}</p>
        <dl class="service-details">{[item.service, item.outcome].map((text, i) => <div><dt>{c.services.labels[i + 1]}</dt><dd>{text}</dd></div>)}</dl>
        <a class="text-link" href={enquiry(item.id)} data-enquiry-service={item.id}>{c.services.cta}<Icon /></a>
      </article>)}
    </div>
  </section>

  <section class="section projects-section" id="projects" aria-labelledby="projects-heading"><div class="shell">
    <SectionHeading id="projects-heading" content={c.projects}>
      <Fragment slot="emphasis">{c.projects.emphasis.split(/(\buseful\b)/).map(part => part === 'useful' ? <span class="projects-heading-metallic">{part}</span> : part)}</Fragment>
    </SectionHeading>
    <div class="project-grid">{c.projects.items.map(item => <article class="project-card"><div class="project-title"><p class="small-note">{c.projects.status}</p></div><h3>{item.heading}</h3><dl>{[item.situation, item.service, item.outcome].map((text, i) => <div><dt>{c.projects.labels[i]}</dt><dd>{text}</dd></div>)}</dl></article>)}</div>
  </div></section>

  <section class="section shell" id="process" aria-labelledby="process-heading">
    <SectionHeading id="process-heading" content={{ heading: c.process.heading, emphasis: c.process.emphasis }}>
      <Fragment slot="emphasis">{c.process.emphasis.split(/(\btogether\b\.?)/).map(part => /^together\.?$/.test(part) ? <span class="process-heading-outline" data-text={part}>{part}</span> : part)}</Fragment>
    </SectionHeading>
    <div class="process-grid" data-process-journey>
      {c.process.items.map((item, index) => <article class="process-card"><span class="process-number">{index + 1}</span><h3>{item.name}</h3><p>{item.description}</p><div class="your-part"><span>{c.process.effortLabel}</span><p>{item.effort}</p></div></article>)}
      <svg class="process-connections" aria-hidden="true" focusable="false">
        <defs>{c.process.items.slice(1).map((_, index) => <linearGradient id={`skills-process-gradient-${index}`} gradientUnits="userSpaceOnUse">
          <stop offset="0%" stop-color="#27cfd0" /><stop offset="30%" stop-color="#5965e8" /><stop offset="44%" stop-color="#9656dc" /><stop offset="59%" stop-color="#e94f87" /><stop offset="78%" stop-color="#f47a45" /><stop offset="100%" stop-color="#f7bd2f" />
        </linearGradient>)}</defs>
        {c.process.items.slice(1).map((_, index) => <path class="process-connection" stroke={`url(#skills-process-gradient-${index})`} pathLength="1" />)}
      </svg>
    </div>
  </section>

  <section class="partners-section shell" id="partners" aria-labelledby="partners-heading" data-content-slot="education-partners">
    <div class="partners-copy"><h2 class="section-heading" id="partners-heading">{c.partners.heading}<br /><span class="editorial">{c.partners.emphasis}</span></h2><div class="partner-tags">{c.partners.tags.map(tag => <span class="tag">{tag}</span>)}</div><a class="button button-dark button-metallic" href={enquiry('partnership')} data-enquiry-service="partnership">{c.partners.cta}<Icon /></a></div>
    <div class="partner-space"><span class="partner-mark" aria-hidden="true">+</span><h3>{c.partners.placeholder}</h3><p>{c.partners.note}</p></div>
  </section>

  <div class="faq-stack">
  <section class="section shell about-section" id="about" aria-labelledby="about-heading">
    <div><h2 class="section-heading" id="about-heading">{c.about.heading.split(/(\bpersonal\b)/).map(part => part === 'personal' ? <span class="about-heading-gradient">{part}</span> : part)}<br /><span class="editorial">{c.about.emphasis}</span></h2></div>
    <div class="people"><figure class="marcel"><div class="portrait-frame"><div class="portrait"><Image src={marcel} alt={c.about.imageAlt} width={480} height={540} widths={[240,360,480]} sizes="(min-width: 768px) 340px, 85vw" format="webp" quality={82} loading="lazy" /></div></div><figcaption><h3>{c.about.name}</h3><p>{c.about.role}</p><p class="small-note">{c.about.bioNote}</p></figcaption></figure><a class="systems-person" href={`${routes.systems}#about`}><span class="portrait-frame portrait-frame-avatar"><LocalPicture src="/images/team/salam-192.webp" alt="" width={192} height={192} /></span><span><strong>{c.about.secondaryName}</strong><span>{c.about.secondaryRole}</span></span><Icon /></a></div>
  </section>

  <section class="section faq-section" id="faq" aria-labelledby="faq-heading"><div class="shell faq-panel">
    <SectionHeading id="faq-heading" content={c.faq}>
      <span slot="heading" class="faq-heading-first">{c.faq.heading}</span>
      <Fragment slot="emphasis">{c.faq.emphasis.split(/(\bclear\b)/).map(part => part === 'clear' ? <span class="faq-heading-gradient">{part}</span> : part)}</Fragment>
    </SectionHeading>
    <div class="faq-list">{c.faq.items.map(item => <details><summary><span class="faq-question">{item.question}</span><FaqToggleIcon /></summary><p>{item.answer}</p></details>)}</div>
  </div></section>
  </div>
  <Contact content={c.contact} services={c.services.items} crossLink={c.crossLink} />
</Layout>
<script>
  import '../../scripts/skills-process';
  const about = document.querySelector<HTMLElement>('.faq-stack .about-section');
  if (about && 'ResizeObserver' in window) {
    const observer = new ResizeObserver(() => {
      about.style.setProperty('--about-height', `${about.offsetHeight}px`);
      about.classList.add('scroll-backdrop');
    });
    observer.observe(about);
  }
  const aboutHeading = document.querySelector<HTMLElement>('.about-heading-gradient');
  if (aboutHeading) {
    if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver(([entry]) => {
        aboutHeading.classList.toggle('is-visible', entry.isIntersecting);
      }, { threshold: .25 });
      observer.observe(aboutHeading);
    } else {
      aboutHeading.classList.add('is-visible');
    }
  }
</script>
<style>
  .skills-hero { padding-top: 38px; }
  .topline { display: flex; justify-content: flex-end; align-items: center; gap: 20px; }

  .hero-grid { display: grid; grid-template-columns: 1.18fr 1fr; align-items: center; gap: 50px; padding-block: 64px 60px; }
  h1 { font-size: clamp(3.4rem, 6.3vw, 6rem); line-height: 1.04; letter-spacing: -.065em; }
  h1 .editorial {
    display: inline-block;
    padding-inline: .06em;
    margin-inline: -.06em;
    background-image: linear-gradient(165deg, transparent 20%, #ffffff14 37%, #ffffff70 46%, #ffffff12 50%, #17172c24 55%, transparent 68%), var(--brand-gradient-interactive);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  @media (forced-colors: active) { h1 .editorial { background: none; color: inherit; opacity: 1; } }
  .hero-description { max-width: 440px; margin-top: 28px; font-size: 17px; line-height: 1.75; color: var(--color-muted); }
  .hero-actions { display: flex; flex-wrap: wrap; align-items: center; gap: 22px; margin-top: 30px; }
  .hero-actions .text-link { font-size: 12px; min-height: 44px; }
  .hero-note { font-size: 11px; color: var(--color-muted); margin-top: 26px; }
  .hero-visual { min-width: 0; }
  figure { margin: 0; }
  .hero-photo { position: relative; border-radius: 18px; overflow: hidden; background: var(--color-surface); }
  .hero-photo :global(img) { display: block; width: 100%; height: auto; aspect-ratio: 1.21; object-fit: cover; object-position: 57% center; }
  .page-nav {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 28px;
  }
  .page-nav a {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    min-width: 0;
    min-height: 60px;
    padding: 21px 0;
    border-top: 1px solid var(--color-line);
    color: var(--color-ink);
    font-size: 12px;
  }
  .page-nav a::after {
    content: '';
    position: absolute;
    top: -1px;
    left: 0;
    right: 0;
    height: 1px;
    background: var(--brand-gradient);
    opacity: 0;
    transition: opacity 220ms ease;
    pointer-events: none;
  }
  .page-nav a:is(:hover, :focus-visible)::after {
    opacity: 1;
  }
  .page-nav-label { display: inline-block; }
  .page-nav a:is(:hover, :focus-visible) .page-nav-label {
    background-image: var(--brand-gradient);
    background-size: 100% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    text-shadow: none;
  }
  @media (forced-colors: active) {
    .page-nav a:is(:hover, :focus-visible) .page-nav-label { background: none; color: LinkText; -webkit-text-fill-color: LinkText; }
  }
  .trust { display: grid; grid-template-columns: 1fr 1.2fr; gap: 40px; align-items: center; padding-block: 44px; border-bottom: 1px solid var(--color-line); }
  .trust h2 { font-size: 19px; letter-spacing: -.025em; }
  .trust-space { min-height: 110px; display: flex; align-items: center; gap: 24px; border: 1px dashed #cbcbc4; border-radius: 12px; padding: 20px 28px; }
  .quote-mark { font-size: 64px; line-height: 1; color: #9299be; }
  .trust-space p { font-size: 14px; }
  .small-note { font-size: 11px !important; line-height: 1.65; color: var(--color-muted); }
  .trust-space .small-note { margin-top: 6px; }
  .needs-section { background: var(--color-paper); }
  .needs-section :global(.section-heading) { font-size: clamp(2.85rem, 5.2vw, 5rem); }
  .needs-heading-gradient {
    display: inline-block;
    padding: .08em .08em .2em;
    margin: -.08em -.08em -.2em;
    background-image: var(--brand-gradient);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  @keyframes needs-gradient-fade {
    0%, 100% { color: var(--color-ink); }
    50% { color: transparent; }
  }
  @media (prefers-reduced-motion: no-preference) and (forced-colors: none) {
    .needs-heading-gradient { animation: needs-gradient-fade 6s ease-in-out infinite; }
  }
  @media (forced-colors: active) {
    .needs-heading-gradient { background: none; color: inherit; }
  }
  @media (max-width: 767px) {
    .needs-section :global(.section-heading) { font-size: clamp(2.65rem, 10vw, 3.65rem); }
  }
  .process-card {
    --card-accent: var(--color-blue);
    position: relative;
    isolation: isolate;
    min-width: 0;
    border: 1px solid transparent;
    border-radius: 22.4px;
    padding: 25.6px;
    display: flex;
    flex-direction: column;
    align-items: start;
    background: var(--process-card-fill, linear-gradient(125deg, #fcfcff, #fffaf8)) padding-box, linear-gradient(125deg, color-mix(in srgb, var(--card-accent) 65%, white), #ece7ff 48%, #ffcbb6) border-box;
    box-shadow: 0 16px 36px #28282605;
  }
  .process-card:nth-child(2) { --card-accent: var(--color-red); }
  .process-card:nth-child(3) { --card-accent: var(--color-orange); }
  .process-card:nth-child(4) { --card-accent: var(--color-purple); }
  .process-card::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    pointer-events: none;
    background: radial-gradient(ellipse at 100% 0%, color-mix(in srgb, var(--card-accent) 16%, transparent), transparent 58%);
  }
  h3 { font-size: 24px; line-height: 1.2; letter-spacing: -.035em; }
  .process-card > p { font-size: 14px; line-height: 1.8; color: var(--color-muted); margin-top: 18px; }
  .text-link :global(svg) { width: 16px; height: 16px; }
  .sample-section {
    --section-fade: clamp(64px, 8vw, 120px);
    background: linear-gradient(to bottom, var(--color-paper), var(--color-surface) var(--section-fade), var(--color-surface) calc(100% - var(--section-fade)), var(--color-paper));
  }
  .sample-grid { display: grid; grid-template-columns: 1.5fr 1fr; gap: 64px; align-items: center; }
  .sample-space { min-height: 400px; background: #eef0fa; border: 1px dashed #bfc6e2; border-radius: 16px; padding: 28px 32px; display: flex; flex-direction: column; justify-content: space-between; align-items: start; }
  .sample-space .tag { border-color: #c7cbe0; color: #525878; }
  .sample-space p { max-width: 410px; color: #62677b; font-size: 13px; line-height: 1.8; margin-top: 12px; }
  .sample-art { display: flex; align-items: center; justify-content: center; width: 100%; height: 160px; }
  .sample-art span { display: block; width: 80px; height: 105px; border: 1px solid #8e9be5; border-radius: 13px; transform: rotate(-14deg); background: #ffffff66; }
  .sample-art span:nth-child(2) { transform: rotate(0); margin-left: -25px; background: #dce1ff; }
  .sample-art span:nth-child(3) { transform: rotate(14deg); margin-left: -25px; background: #eff0ff; }
  .sample-steps { list-style: none; padding: 0; margin: 0; }
  .sample-steps li { display: flex; gap: 24px; padding: 23px 0; border-bottom: 1px solid #ddddd5; }
  .sample-steps li:first-child { padding-top: 0; }
  .sample-steps h3 { font-size: 20px; }
  .sample-steps p { color: var(--color-muted); font-size: 13px; line-height: 1.8; margin-top: 8px; }
  #services :global(.section-intro) { text-align: center; }
  .services-heading-main { font-size: 1.15em; font-weight: 700; line-height: 1.12; }
  .services-heading-metallic, .projects-heading-metallic {
    display: inline-block;
    background-image: linear-gradient(165deg, transparent 20%, #ffffff14 37%, #ffffff70 46%, #ffffff12 50%, #17172c24 55%, transparent 68%), var(--brand-gradient-interactive);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .projects-heading-metallic { padding: 0 .06em .2em; margin: 0 -.06em -.2em; }
  @media (forced-colors: active) { .services-heading-metallic, .projects-heading-metallic { background: none; color: inherit; } }
  .services-grid { display: grid; grid-template-columns: repeat(4,minmax(0,1fr)); gap: 19.2px; }
  .service-card {
    --card-accent: var(--color-blue);
    position: relative;
    isolation: isolate;
    min-width: 0;
    border: 1px solid transparent;
    border-radius: 22.4px;
    padding: 25.6px;
    display: flex;
    flex-direction: column;
    background: linear-gradient(125deg, #fcfcff, #fffaf8) padding-box, linear-gradient(125deg, color-mix(in srgb, var(--card-accent) 65%, white), #ece7ff 48%, #ffcbb6) border-box;
    box-shadow: 0 16px 36px #28282605;
  }
  .service-card::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    pointer-events: none;
    background: radial-gradient(ellipse at 100% 0%, color-mix(in srgb, var(--card-accent) 16%, transparent), transparent 58%), repeating-linear-gradient(125deg, #ffffff50 0, #ffffff00 24px, #ffffff50 52px);
  }
  .service-gradient {
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    pointer-events: none;
    background: var(--brand-gradient-interactive);
    opacity: 0;
    transition: opacity 240ms ease;
  }
  .service-card::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    pointer-events: none;
    background: linear-gradient(180deg, transparent 35%, #aeb4c414 42%, #ffffffcc 48%, #ffffffee 50%, #aeb4c426 54%, transparent 63%);
    background-size: 100% 300%;
    background-position: 0 100%;
    opacity: 0;
  }
  @keyframes service-metallic-slide {
    0% { background-position: 0 100%; opacity: 0; }
    15%, 80% { opacity: 1; }
    100% { background-position: 0 0; opacity: 0; }
  }
  @media (prefers-reduced-motion: no-preference) {
    .service-card::after { animation: service-metallic-slide 7s linear infinite; }
  }
  .red { --card-accent: var(--color-red); } .orange { --card-accent: var(--color-orange); } .purple { --card-accent: var(--color-purple); }
  .service-card h3 { font-size: clamp(25.6px, 2.8vw, 40px); line-height: 1.08; min-height: 2.16em; letter-spacing: -.055em; font-weight: 700; color: #18181b; }
  .service-description { margin-top: 17.6px; min-height: 4.95em; font-size: 12.8px; line-height: 1.65; color: #686872; max-width: 54ch; }
  dl { margin: 22px 0 0; }
  dl > div { padding-block: 15px; border-top: 1px solid var(--color-line); }
  dt { text-transform: uppercase; font-size: 9px; font-weight: 500; letter-spacing: .08em; }
  dd { margin: 8px 0 0; font-size: 12px; color: var(--color-muted); line-height: 1.8; }
  .service-details { display: grid; grid-template-columns: 1fr; grid-template-rows: repeat(2, 1fr); flex: 1; gap: 19.2px; margin: 22.4px 0 16px; border-top: 1px solid #dedde3; }
  .service-details > div { border: 0; padding: 16px 0 0; }
  .service-details dt { color: #55555e; }
  .service-details dd { margin-top: 6.4px; font-size: 11px; color: #686872; }
  .service-card a { margin-top: auto; padding-block: 9.6px 3.2px; padding-right: 16px; min-height: 44px; font-size: 11px; justify-content: space-between; }
  .service-card a :global(svg) { width: 15.2px; height: 15.2px; }
  .service-card:target { outline: 2px solid var(--card-accent); outline-offset: 4px; }
  .service-card:is(:hover, :focus-within) .service-gradient { opacity: 1; }
  .service-card:is(:hover, :focus-within) :is(h3, .service-description, dt, dd, a) { color: white; }
  .service-card:is(:hover, :focus-within) .service-details { border-color: #ffffff80; }
  .service-card:is(:hover, :focus-within) a { text-decoration-color: #ffffff99; }
  .service-card:is(:hover, :focus-within)::after { background-image: linear-gradient(180deg, transparent 35%, #ffffff0a 42%, #ffffff26 48%, #ffffff38 50%, #ffffff0d 54%, transparent 63%); }
  .service-card a:focus-visible { outline-color: white; }
  .project-grid { display: grid; grid-template-columns: repeat(2,minmax(0,1fr)); gap: 28px; }
  .project-card {
    position: relative;
    isolation: isolate;
    padding: 32px;
    border: 1px solid transparent;
    border-radius: 14px;
    background: linear-gradient(var(--color-surface), var(--color-surface)) padding-box, linear-gradient(110deg, #a5aaff, #e8dcff 30%, #f8dce9 68%, #f9e5db) border-box;
  }
  .project-card::before, .partners-section::before {
    content: '';
    position: absolute;
    inset: -3px;
    z-index: -2;
    border-radius: inherit;
    pointer-events: none;
    background: conic-gradient(from 90deg, #e94f87, #f47a45, #f7bd2f, #27cfd0, #5965e8, #9656dc, #e94f87);
    filter: blur(7px);
    opacity: 0.25;
  }
  .project-card::after, .partners-section::after {
    content: '';
    position: absolute;
    inset: 0;
    z-index: -1;
    border-radius: inherit;
    pointer-events: none;
    background: var(--box-surface, var(--color-surface));
  }
  .project-title { margin-bottom: 25px; }

  .project-card dl > div { display: grid; grid-template-columns: 135px 1fr; gap: 20px; }
  .project-card dt { padding-top: 3px; } .project-card dd { margin: 0; }
  #process { --process-card-fill: linear-gradient(125deg, #fcfcff, #fffaf8); }
  #process :global(.section-intro) { text-align: center; margin-bottom: 32px; transform: translateY(-38px); }
  #process :global(.section-heading) { font-size: calc(clamp(2.65rem, 4.8vw, 4.6rem) + 2px); text-wrap: balance; }
  @media (max-width: 767px) {
    #process :global(.section-intro) { margin-bottom: 24px; }
    #process :global(.section-heading) { font-size: calc(clamp(2.5rem, 9.5vw, 3.6rem) + 2px); }
  }
  .process-heading-outline {
    display: inline-block;
    position: relative;
    vertical-align: baseline;
    padding: .08em .2em .2em .08em;
    margin: -.08em -.12em -.2em -.08em;
    background-image: var(--brand-gradient);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    -webkit-text-stroke: 1.5px transparent;
  }
  .process-heading-outline::after {
    content: attr(data-text) / '';
    position: absolute;
    inset: 0;
    padding: inherit;
    background-image: var(--process-card-fill);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-stroke: 0;
    pointer-events: none;
  }
  @keyframes process-heading-fill-fade {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
  @media (prefers-reduced-motion: no-preference) {
    .process-heading-outline::after { animation: process-heading-fill-fade 6s ease-in-out infinite; }
  }
  @media (forced-colors: active) {
    .process-heading-outline { background: none; color: inherit; -webkit-text-stroke: 0; }
    .process-heading-outline::after { content: none; }
  }
  .process-grid { --step-offset: 32px; position: relative; isolation: isolate; display: grid; grid-template-columns: repeat(4,minmax(0,1fr)); grid-auto-rows: 1fr; gap: 104px 96px; padding-bottom: var(--step-offset); }
  .process-card { align-items: stretch; z-index: 1; width: 100%; max-width: 320px; justify-self: center; padding: 20px; }
  .process-card:nth-child(even) { top: var(--step-offset); }
  .process-connections { position: absolute; inset: 0; width: 100%; height: 100%; overflow: visible; pointer-events: none; z-index: 0; }
  .process-connection { fill: none; stroke-width: 3; stroke-linecap: round; }
  @media (prefers-reduced-motion: no-preference) {
    .process-grid.is-sequenced .process-card { opacity: 0; transform: translateY(16px); }
    .process-grid.is-sequenced .process-card.is-revealed { opacity: 1; transform: none; }
    .process-grid.is-sequenced .process-connection { stroke-dasharray: 1; stroke-dashoffset: 1; opacity: 0; }
    .process-grid.is-sequenced .process-connection.is-drawn { stroke-dashoffset: 0; opacity: 1; }
  }
  @media (forced-colors: active) {
    .process-connection { stroke: CanvasText; }
  }
  .process-card h3 { font-size: 20px; }
  .process-number { align-self: flex-start; display: grid; place-items: center; width: 28px; height: 28px; margin-bottom: 14px; border: 1px solid color-mix(in srgb, var(--card-accent) 35%, white); border-radius: 50%; color: var(--color-ink); font-size: 13px; font-weight: 500; }
  .process-card > p { font-size: 13px; line-height: 1.7; margin-top: 14px; margin-bottom: 20px; }
  .your-part { margin-top: auto; padding-top: 18px; border-top: 1px solid var(--color-line); }
  .your-part span { font-size: 10px; text-transform: uppercase; letter-spacing: .08em; }
  .your-part p { font-size: 12px; line-height: 1.8; color: var(--color-muted); margin-top: 8px; }
  .partners-section { --box-surface: var(--color-accent-soft); position: relative; isolation: isolate; display: grid; grid-template-columns: 1.3fr 1fr; gap: 56px; padding: 56px; border: 1px solid transparent; border-radius: 18px; background: linear-gradient(var(--box-surface), var(--box-surface)) padding-box, linear-gradient(110deg, #a5aaff, #e8dcff 30%, #f8dce9 68%, #f9e5db) border-box; }

  .partners-copy h2 { font-size: clamp(2.5rem,3.6vw,3.6rem); }
  .body-copy { font-size: 15px; line-height: 1.8; color: var(--color-muted); margin-top: 24px; max-width: 530px; }
  .partner-tags { display: flex; flex-wrap: wrap; gap: 8px; margin-block: 24px; }
  .partner-tags .tag { border-color: #cbd0e1; }
  .partner-space { align-self: stretch; border: 1px dashed #b8bfd8; border-radius: 12px; display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 36px; text-align: center; min-height: 320px; }
  .partner-mark { font-size: 58px; color: #828ba9; margin-bottom: 24px; font-weight: 400; }
  .partner-space h3 { font-size: 23px; max-width: 240px; }
  .partner-space p { margin-top: 14px; font-size: 12px; color: #62677b; line-height: 1.8; max-width: 230px; }
  .about-section { display: grid; grid-template-columns: 1.3fr 1fr; gap: 90px; align-items: center; }
  .about-heading-gradient { display: inline-block; background-image: var(--brand-gradient-interactive); -webkit-background-clip: text; background-clip: text; color: transparent; }
  @media (prefers-reduced-motion: no-preference) and (forced-colors: none) {
    .about-heading-gradient {
      background-image: linear-gradient(115deg, transparent 18%, #ffffff24 34%, #ffffff70 44%, #ffffff70 48%, #ffffff24 58%, transparent 70%), var(--brand-gradient-interactive);
      background-size: 200% 100%, 100% 100%;
      background-repeat: no-repeat;
      background-position: -100% 0, 0 0;
      animation: about-metallic-slide 7s linear infinite paused;
    }
    .about-heading-gradient.is-visible { animation-play-state: running; }
    @keyframes about-metallic-slide {
      0%, 10% { background-position: -100% 0, 0 0; }
      90%, 100% { background-position: 200% 0, 0 0; }
    }
  }
  @media (forced-colors: active) { .about-heading-gradient { background: none; color: inherit; } }
  .people { width: min(100%,340px); justify-self: center; }
  .portrait { border-radius: 14px; overflow: hidden; background: var(--color-surface); }
  .portrait :global(img) { width: 100%; height: auto; }
  @media (hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference) {
    .portrait :global(img) { transition: transform 500ms cubic-bezier(.22, 1, .36, 1), filter 400ms ease; }
    .portrait:hover :global(img) { transform: scale(1.035); filter: saturate(1.05) contrast(1.02); }
  }
  .marcel figcaption { padding-block: 22px; }
  .marcel figcaption h3 { font-size: 22px; }
  .marcel figcaption p { font-size: 12px; color: var(--color-muted); margin-top: 8px; }
  .marcel figcaption .small-note { margin-top: 14px; }
  .systems-person { display: flex; align-items: center; gap: 14px; border-top: 1px solid var(--color-line); padding-top: 18px; }
  .systems-person :global(img) { width: 50px; height: 50px; object-fit: cover; border-radius: 50%; }
  .systems-person strong { font-size: 13px; font-weight: 500; }
  .systems-person span span { display: block; margin-top: 5px; font-size: 10px; color: var(--color-muted); }
  .systems-person > :global(svg) { width: 17px; margin-left: auto; }
  .faq-stack { position: relative; isolation: isolate; }
  .faq-section { position: relative; z-index: 1; }
  /* Cover the preceding section as the bottom of the black panel scrolls away. */
  .faq-section::after { content: ''; position: absolute; z-index: -1; inset: 50% 0 0; background: var(--color-paper); pointer-events: none; }
  @media (prefers-reduced-motion: no-preference) {
    .about-section.scroll-backdrop {
      position: sticky;
      /* Let tall content finish scrolling before holding it behind the FAQ. */
      top: min(calc(var(--header-top) + var(--header-height) + 16px), calc(100svh - var(--about-height) - 24px));
    }
  }
  .faq-panel {
    --color-ink: #fff;
    --color-muted: #fff;
    --color-line: #ffffff33;
    position: relative;
    isolation: isolate;
    padding: clamp(24px, 4vw, 56px);
    border-radius: 22.4px;
    color: #fff;
  }
  .faq-panel::before {
    content: '';
    position: absolute;
    z-index: -1;
    inset: -5%;
    inset-inline: max(-5%, calc((100% - 100vw) / 2 + 20px));
    border-radius: inherit;
    background: #18181b;
    box-shadow: 0 -12px 32px #2828260d;
    pointer-events: none;
  }
  .faq-list { max-width: 800px; margin-inline: auto; }
  .faq-panel :global(#faq-heading) {
    font-size: clamp(2.75rem, 5.2vw, 5.25rem);
    font-weight: 700;
    line-height: 1.16;
    letter-spacing: -.04em;
  }
  .faq-heading-first {
    display: inline-block;
    margin-inline-start: calc(-1 * clamp(16px, 3vw, 48px));
  }
  .faq-heading-gradient { display: inline-block; padding: .06em .08em .2em; margin: -.06em -.08em -.2em; background-image: var(--brand-gradient); -webkit-background-clip: text; background-clip: text; color: transparent; }
  @media (forced-colors: active) { .faq-heading-gradient { background: none; color: inherit; } }
  .faq-panel :global(#faq-heading .editorial) {
    display: inline-block;
    margin-top: .14em;
    margin-inline-start: clamp(12px, 4vw, 56px);
    max-width: calc(100% - clamp(12px, 4vw, 56px));
    font-weight: 600;
    letter-spacing: -.035em;
  }
  details { border-top: 1px solid var(--color-line); } details:last-child { border-bottom: 1px solid var(--color-line); }
  summary { list-style: none; cursor: pointer; display: flex; align-items: center; justify-content: space-between; gap: 24px; padding-block: 23px; font-size: 18px; font-weight: 700; line-height: 1.5; }
  summary::-webkit-details-marker { display: none; }
  details[open] .faq-question {
    background-image: var(--brand-gradient);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  @media (forced-colors: active) {
    details[open] .faq-question { background: none; color: CanvasText; }
  }
  details > p { padding: 0 35px 25px 0; font-size: 14px; font-weight: 400; color: var(--color-muted); line-height: 1.8; }
  @media (hover: hover) and (pointer: fine) { .service-card { transition: box-shadow 180ms ease; } .service-card:hover { box-shadow: 0 12px 32px color-mix(in srgb, var(--card-accent) 12%, transparent); } }
  @media (hover: hover) and (pointer: fine) and (prefers-reduced-motion: no-preference) {
    .service-card {
      translate: 0 0;
      transition: box-shadow 180ms ease, translate 500ms cubic-bezier(.22, 1, .36, 1), opacity 700ms cubic-bezier(.22, 1, .36, 1), transform 700ms cubic-bezier(.22, 1, .36, 1);
    }
    .service-card:hover { translate: 0 -3px; }
  }
  @media (max-width: 1100px) { .hero-grid { gap: 32px; } h1 { font-size: clamp(3.1rem,6.2vw,5rem); } .services-grid, .process-grid { grid-template-columns: repeat(2,minmax(0,1fr)); } .service-card h3 { min-height: auto; } .partners-section { padding: 36px; gap: 32px; } .about-section { gap: 40px; } .sample-grid { gap: 32px; } }
  @media (max-width: 767px) {
    .services-grid { grid-template-columns: 1fr; gap: 16px; max-width: 400px; margin-inline: auto; }
    .service-card { padding: 22.4px; }
    .service-card h3, .service-description { min-height: 0; }
    .skills-hero { padding-top: 25px; } .topline { align-items: start; } .topline .draft-note { font-size: 9px; text-align: right; }
    .hero-grid { grid-template-columns: 1fr; padding-block: 44px 20px; gap: 40px; } h1 { font-size: clamp(3.1rem,11vw,5.5rem); } .hero-description { font-size: 16px; }
    .hero-photo :global(img) { aspect-ratio: 1.45; }
    .page-nav { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px 24px; }
    .page-nav a { padding: 17px 0; font-size: 11px; }
    .trust { grid-template-columns: 1fr; gap: 22px; padding-block: 32px; } .trust-space { padding: 20px; }
    .sample-grid, .project-grid, .partners-section, .about-section { grid-template-columns: 1fr; }
    .sample-space { min-height: 350px; padding: 24px; } .sample-art { height: 140px; } .sample-steps li:first-child { padding-top: 10px; }
    .project-card { padding: 24px; } .project-card dl > div { grid-template-columns: 1fr; gap: 8px; }
    .partners-section { padding: 32px 24px; gap: 32px; } .partner-space { min-height: 250px; } .about-section { gap: 36px; } .people { justify-self: start; }
    .faq-heading-first { margin-inline-start: -4px; }
    .process-grid { --step-offset: 0px; grid-template-columns: 1fr; grid-auto-rows: auto; gap: 96px; max-width: 320px; margin-inline: auto; }
  }
  @media (max-width: 479px) { .services-grid, .process-grid { grid-template-columns: 1fr; } .service-card { padding: 19.2px; }  .service-details { gap: 0; }  .process-card > p { margin-bottom: 18px; } }
</style>
