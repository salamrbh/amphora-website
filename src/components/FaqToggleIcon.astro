<span class="faq-toggle-icon" aria-hidden="true">
  <svg width="18" height="18" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" shape-rendering="geometricPrecision">
    <path d="M3 9h12M9 3v12" />
  </svg>
</span>

<style>
  .faq-toggle-icon {
    display: grid;
    place-items: center;
    width: 30px;
    height: 30px;
    flex: none;
    border-radius: 50%;
    color: #fff;
    background: #18181b;
  }
  :global(details[open]) .faq-toggle-icon {
    background: radial-gradient(circle at 35% 20%, #ffffff70 0%, #ffffff20 45%, #ffffff00 80%), var(--brand-gradient);
    box-shadow: inset 0 1px 3px #ffffff40, 0 2px 8px #9656dc26;
  }
  .faq-toggle-icon svg { display: block; width: 18px; height: 18px; transform-origin: center; }
  :global(details[open]) .faq-toggle-icon svg { transform: rotate(45deg); }
  @media (prefers-reduced-motion: no-preference) {
    .faq-toggle-icon svg { transition: transform 200ms ease; }
  }
  @media (forced-colors: active) {
    .faq-toggle-icon, :global(details[open]) .faq-toggle-icon { background: Canvas; color: CanvasText; border: 1px solid CanvasText; box-shadow: none; }
  }
</style>
